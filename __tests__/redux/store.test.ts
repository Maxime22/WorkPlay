// redux/store.test.ts
import store from '../../redux/store';

describe('Redux Store', () => {
  it('should have a valid initial state', () => {
    // Placeholder test to verify the store setup.
    // All slices (reducers) are tested individually, so no additional tests are needed here.
    expect(store.getState()).toBeDefined();
  });
});
