import React from 'react';
import {Text, View} from 'react-native';

const WorkPlayApp = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Hello, Lulu!</Text>
    </View>
  );
};
export default WorkPlayApp;